// Buttons
sortButton = document.querySelector(".submit-button .rainbow button");
sortButton.addEventListener("click", function (event) {
  event.preventDefault();
  console.log("Sort button clicked");
});
